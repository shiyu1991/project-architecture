---
name: project-architecture
description: >-
  使用场景：初始化新项目、选择技术栈、设计或演进项目架构，或使用 AI
  编程助手（OpenAI Codex / Claude Code / CodeBuddy / Cursor Agent /
  OpenCode / Devin）进行长期开发时。适用于非技术用户、产品经理和开发者，自动适配交互方式。
---

# AI Native Project Architecture Skill V6.0

> 企业级 AI Coding Agent 项目初始化、架构设计、持续开发规范。

---

## 目录

| 章节 | 内容 | 加载时机 | 给谁看 |
|------|------|----------|--------|
| 一、Skill 定位 | 核心目标、能力边界、使用模式 | 始终 | 所有人 |
| 二、三档用户模式 | 技术小白 / 产品经理 / 开发者 | 开场必做 | 所有人 |
| 三、规模分级与类型判定 | S/M/L 反过度工程 + 项目类型分支 | 开场必做 | 所有人 |
| 四、最高执行原则 | AI Agent 8 步执行流程 | 始终 | 开发者 |
| 五、项目上下文管理 | `.agent/` 四件套 | 进入项目时 | 开发者 |
| 六、需求分析规范 | 业务→领域→应用→技术→基础设施 | 需求分析时 | 产品经理 |
| 七、技术选型总览 | 四阶段导航 + 核心红线（详细协议见 execution-protocol.md） | 技术选型时 | 开发者 |
| 八、项目整体架构 | Core + DDD 结构概览（详细规范见 execution-protocol.md） | 架构设计时 | 开发者 |
| 九、AI 开发生命周期 | 6 阶段流程 | 编码时 | 开发者 |
| 十、关键红线 | 测试 / 安全 / Core / 删除 / README | 始终 | 所有人 |
| 十一、何时不使用与 FAQ | 边界界定 + 常见问题 | 开场判断 | 所有人 |
| 十二、参考文档索引 | references / templates 加载时机 | 按需 | 所有人 |

> **快速定位：** 不懂技术→一二三章；产品经理→二六十一；开发者→全篇按需。

> **3 秒决策树（不知道说什么？看这里）：** 你只需说一句话，AI 会自动识别模式并启动流程，不需要预先选择模式 / 规模 / 类型。
>
> ```
> 你说的话                              → AI 启动模式    → 流程入口
> ─────────────────────────────────    ──────────────  ─────────────────
> "我想做 XX，但我不懂技术"            → 🌱 引导模式    → 第二章 → 规模分级 → 选型
> "这是 PRD，帮我立项"                 → 📋 产品模式    → 第六章 → 领域建模 → 选型
> "帮我初始化一个 React + NestJS 项目" → ⚡ 专家模式    → 第七章 → 逐层选型
> "分析这个仓库的技术栈并给优化建议"   → 🔍 反向分析    → 反向分析流程（不重新选型）
> ```
>
> 不确定说哪个？直接描述你想做的事（哪怕只一句"做个待办 app"），AI 会自动识别模式并问后续问题，全程只需按问题回答即可。

> **环境与工具适配（国内友好）：** 本 Skill 核心流程不依赖境外网络即可完成，各环节均有国内回退方案。
>
> | 环节 | 标准方式 | 国内回退方案（标准方式不可用时） |
> |------|----------|----------------------------------|
> | 交互式选型 | `ask_followup_question`（CodeBuddy / Claude Code 等支持） | 回退为正文逐层列候选（标注"推荐"+理由），用户回复编号/名称确认 |
> | 版本实时查询 | `web_search` 搜索官方源 | 回退到 `references/tech-stack-guide.md` 内置版本基线，标注"查询失败，版本可能滞后，请用户复核" |
> | 依赖安装（Node） | npm 官方源 | `npmmirror.com`（原 cnpm 镜像） |
> | 依赖安装（Python） | pip 官方 PyPI | 清华源 `pypi.tuna.tsinghua.edu.cn/simple` 或阿里源 `mirrors.aliyun.com/pypi/simple/` |
> | 依赖安装（PHP） | Composer 官方 | 阿里云源 `mirrors.aliyun.com/composer/` |
> | 依赖安装（Java） | Maven Central | 阿里云源 `maven.aliyun.com/repository/public`；Gradle 同步配置 |
> | 依赖安装（Go） | proxy.golang.org | `goproxy.cn`（七牛）或 `goproxy.io`；`go env -w GOPROXY=https://goproxy.cn,direct` |
> | 依赖安装（Rust） | crates.io | 字节镜像 `rsproxy.cn`；配置 `~/.cargo/config.toml` 的 `[source.crates-io]` 与 `[registries.rsproxy]` |
> | 依赖安装（Ruby） | rubygems.org | 清华源或 Ruby China `gems.ruby-china.com` |
> | Docker 镜像拉取 | docker.io（Docker Hub） | 国内镜像加速器：阿里云 / 网易 / DaoCloud `docker.m.daocloud.io`；配置 `~/.docker/daemon.json` 的 `registry-mirrors` |
> | Git 克隆 | github.com 直接克隆 | `ghproxy.com` 代理（`https://ghproxy.com/https://github.com/...`）或 `gitclone.com` 镜像，标注"使用代理加速" |
> | 代码托管 | GitHub / GitLab | 国内可选 Gitee（gitee.com）、CODING（coding.net）、自建 GitLab；选型候选须并列展示，由用户决定 |
> | AI 模型 API | OpenAI / Anthropic / Google 等境外 API | 优先选国产替代：DeepSeek（deepseek.com）、智谱 GLM（bigmodel.cn）、通义千问（dashscope）、文心一言（百度）、Kimi（moonshot）；通过统一 Core 抽象隔离，支持按需切换 |
> | 包发布 | npm / PyPI / Maven Central 发布 | 私有 registry：cnpm（自建）、阿里云制品仓库；如需公开发布仍需境外账号 |
> | 部署目标 | AWS / Vercel / Heroku 等 | 阶段四部署候选**必须**包含腾讯云 / 阿里云 / 华为云，与境外方案并列供用户选择；静态站点首选腾讯云 CloudBase / 阿里云 OSS+CDN |
> | 域名与备案 | 境外域名 + Cloudflare DNS | 国内部署必须使用已备案域名（管局 ICP 备案），DNS 可选 DNSPod（腾讯）/ 阿里云 DNS |
>
> **原则：** 任何环节因网络或工具不可用时，必须回退到上表方案并告知用户，禁止因环境问题中断整个流程。涉及 AI 模型 API 时，国产替代与境外方案须并列供用户选择，由用户决定。

---

# 一、Skill 定位

指导 AI Agent 按资深技术架构师标准完成：项目初始化、技术选型、架构设计、模块规划、代码开发、项目维护、架构演进。

核心目标：技术选型合理、业务与技术解耦、公共能力复用、降低系统复杂度、提升 AI 持续开发能力、保证长期可维护性。

核心原则：

- 先理解，再设计
- 先架构，再编码
- 先抽象，再实现
- 先复用，再扩展
- 先验证，再提交

## 快速开始（可直接复制的触发语）

- "我想做一个宠物寄养小程序，但我不懂技术" → 引导模式全流程立项
- "这是我们团队的产品 PRD，帮我立项" → 产品模式业务梳理
- "帮我初始化一个 React + NestJS 的中后台项目" → 专家模式快速选型
- "分析一下这个仓库的技术栈并给出优化建议" → 已有项目反向分析

完整端到端示例（触发→执行→输出）见 `references/examples.md`。

## 能力边界

| 分类 | 内容 |
|------|------|
| ✅ 擅长 | 新项目初始化、技术选型、架构设计、模块规划、已有项目反向分析、持续开发规范 |
| 🤝 需用户配合 | 逐层回答选型问题（或授权 AI 自动选择）、确认需求分析与领域模型草图 |
| ❌ 不处理 | 替代人工架构评审、编写具体业务代码、回答与项目架构无关的问题 |

### 边界情况明细（避免用户自行摸索的灰色地带）

| 边界场景 | 处理方式 |
|----------|----------|
| **规模升级**（S→M、M→L，例如脚本演变成正式产品） | 用户提出后立即重新评估：补做对应级别门禁（M 补 `.agent/context.md`，L 补全 `.agent/` 四件套 + ADR + Core 结构）；已生成的代码不重写，仅补缺漏的产物；记录到 `.agent/architecture.md` 的"演进记录"小节 |
| **规模降级**（L→M、M→S，例如团队解散或项目归档） | 不强制拆除已有架构；标记当前级别为"维持现状 + 降级目标"，新功能按降级目标门禁执行，旧产物作为历史记录保留 |
| **用户模式切换**（引导→专家，例如用户学习后想自己决定） | 用户主动声明即可切换；选型流程不变，AI 把"推荐项 + 解释"改为"候选清单"输出密度；不推翻已选清单，从下一轮开始按新模式进行 |
| **选型完成后引入未选型技术**（如团队后期加了 Redis、RabbitMQ） | 走"补选流程"：①确认是新增层级还是替换已选方案 ②如新增层级，单独走该层级的选型协议（动态调研 + 候选 + 用户确认）③更新 `.agent/architecture.md` 与（如为 L 级）ADR ④禁止静默引入未走选型协议的依赖 |
| **跨项目复用配置**（多个项目想共用同一套技术栈） | 把技术栈汇总提取到独立的 `tech-stack-profile.md`，作为新项目的预设基线；新项目仍走选型流程，但可"基于预设 + 增量差异"模式加速，不跳过用户确认 |
| **多 Agent 协作分工** | 推荐"按业务模块划分 + ADR 同步跨模块决策"；Core 层只允许主 Agent 修改，其他 Agent 通过 ADR 提议；并行选型禁止，避免技术栈冲突（见 `execution-protocol.md` 第五章进度文件并发处理） |
| **项目演进入口**（已选型项目想升级架构、引入新理念） | 走"反向分析 + 选型差异"流程：先用反向分析评估现状，列出差距清单，再按差距逐项走选型变更流程；禁止整体推翻重选 |
| **用户引入 AI 模型时跨平台兼容** | 境外模型（OpenAI/Anthropic/Google）与国产模型（DeepSeek/智谱/通义/文心/Kimi）须在选型时同时列出，由用户决定；通过 Core 层抽象统一接口，禁止业务层直接 import 特定厂商 SDK |
| **国内项目硬性合规边界**（ICP 备案、数据出境、等保） | 部署在国内的服务必须使用已备案域名；用户数据原则上不出境，涉及等保 2.0 的项目应在 L 级门禁中体现；本 Skill 不替代合规咨询，仅做提示，由用户自行确认法律边界 |

> 上述明细遇到未列出的新场景，AI 必须按"先告知边界、再问用户决定"处理，禁止静默决策。

## 使用模式

| 模式 | 场景 | 说明 |
|------|------|------|
| 完整立项 | 从零开始的项目 | 需求分析 → 选型 → 架构 → 初始化验收 |
| 快速选型 | 仅需技术栈决策 | 跳过需求分析，直接逐层选型 |
| 反向分析 | 已有项目 | 评估技术栈 / 架构 / 质量，输出优化建议 |
| 精简流程 | S 级脚本 / 原型 | 理解 → 实现 → 验证 → 总结 |

---

# 二、三档用户模式（开场必做）

AI Agent 首次接触用户时，必须先用一句话确认用户类型，并切换到对应交互模式：

| 模式 | 用户画像 | 交互方式 |
|------|----------|----------|
| 🌱 引导模式 | 技术小白 / 非技术人员 | 全程大白话，每层给出"推荐项 + 一句人话解释"，用户只需回答"用推荐"即可完成全部选型；不解释术语除非用户问 |
| 📋 产品模式 | 产品经理 / 业务方 | 聚焦业务需求梳理（用户、流程、数据、边界），输出领域模型草图；技术选型只给结论清单和理由摘要，不逐层打断 |
| ⚡ 专家模式 | 开发者 / 技术负责人 | 逐层确认关键分歧点，推荐项标注"推荐"供快速确认，每层仍需用户勾选，快速输出完整技术栈清单 |

模式判定信号：

- 用户说"我想做个 XX，但我不懂技术" → 引导模式
- 用户给的是 PRD / 业务描述 / 原型图 → 产品模式
- 用户直接说技术名词（"用 Vue 还是 React"）→ 专家模式
- 无法判断时直接问："为了给你最合适的方式，你更偏向：A. 我不懂技术，你帮我定 B. 我懂业务，技术你推荐 C. 我是开发者，咱们快速过"

---

# 三、规模分级与类型判定（开场必做）

## 3.1 规模分级（反过度工程）

**不是所有项目都需要完整架构。** 开工前必须同时判定项目规模与风险，禁止给小项目套大架构，也禁止低估高风险项目：

| 级别 | 判定信号 | 必备产物与门禁 |
|------|----------|----------------|
| S 级：脚本 / 原型 / 一次性工具 | 单文件可完成、无部署需求、用完即弃、无敏感数据 | 最小 `README.md`；完成启动/核心行为验证；精简版"理解 → 实现 → 验证 → 总结" |
| M 级：小型应用 / 个人项目 | 1-3 个功能模块、单人开发、生命周期 < 6 个月 | `README.md` + 按需 `.agent/context.md`；至少单元测试或核心流程验证；"理解 → 分析 → 设计 → 实现 → 验证 → 总结" |
| L 级：正式产品 / 团队协作 | 多模块、多人协作、长期维护、有部署运维 | 完整 `.agent/` 四件套 + `docs/adr/`；Core + 业务模块；完整六阶段、CI、安全和发布门禁 |

**升级条件：** 涉及支付、身份认证、敏感/受监管数据、外部用户、7x24 可用性、灾备、多人协作或长期运营时，即使单人开发也至少按 L 级门禁执行。规模或风险发生变化时必须重新评估。

**默认假设为 M 级、低风险。** 只有在用户确认或风险信号明确时升级架构与门禁；AI Agent 必须先展示判定结果和影响，再继续。

## 3.2 项目类型判定（选型分支）

规模分级完成后，判定项目类型以确定选型流程分支：

| 类型 | 判定信号 | 选型流程 |
|------|----------|----------|
| 全栈 Web | 前端 + 后端均需要 | 阶段一（核心选择）→ 阶段二（前端全部层级）→ 阶段三（后端全部层级）→ 阶段四（跨端关注点） |
| 纯前端 | 无后端 / 仅用 BFF / 静态站 / SPA | 阶段一（仅前端项 + 部署方案）→ 阶段二 → 阶段四 |
| 纯后端 API | 无前端 / 仅提供 API 服务 | 阶段一（仅后端项 + 数据库 + 部署方案）→ 阶段三 → 阶段四 |
| 小程序 / 移动端 | 微信小程序 / UniApp / Flutter / React Native | 移动端选型流程（见 `references/tech-stack-guide.md` 移动端章节） |
| 桌面端应用 | Electron / Tauri / 桌面客户端 | 阶段一（前端主框架 + 桌面框架）→ 阶段二 → 阶段四（含桌面打包/自动更新） |
| AI / ML 应用 | AI 推理 / 训练 / 数据分析 | 阶段一（Python 优先）→ 阶段三（AI 框架见 tech-stack-guide）→ 阶段四 |

**判定时机：** 在用户模式识别后、规模分级后、技术选型前。判定后明确告知用户项目类型和选型流程分支。

**跳过规则：** 被跳过的端不需要逐层勾选，AI Agent 直接在选型结果汇总中标注"本项目不需要"即可。

---

# 四、AI Agent 最高执行原则

任何 AI Agent 进入项目，必须按以下流程执行：

1. 阅读项目文档 → 2. 分析项目结构 → 3. 理解已有架构 → 4. 检查已有能力 → 5. 设计修改方案 → 6. 执行开发 → 7. 验证结果 → 8. 输出总结

禁止：

- 未理解项目直接写代码
- 根据一句需求直接生成完整功能
- 重复创建已有能力
- 随意修改核心架构
- 无理由增加技术依赖
- 大范围无目的重构

---

# 五、项目上下文管理

AI Agent 按项目级别优先读取已有上下文：

```
.agent/
├── context.md          # 项目定位与业务背景
├── architecture.md     # 架构决策与结构说明
├── coding-rule.md      # 编码规范
└── workflow.md         # 开发工作流

docs/adr/               # 架构决策记录（项目根目录）
```

以及：`README.md`、项目目录结构、依赖配置文件（`package.json` / `pom.xml` / `go.mod` / `requirements.txt`）。缺失的可选文件不得阻塞 S/M 级项目。

AI 必须明确：项目定位、业务目标、技术架构、核心模块、公共能力、技术约束、部署方式、潜在风险。

**新项目初始化时**：S 级只用 README 模板；M 级使用 README，并按需生成 `.agent/context.md`；L 级用 `templates/` 生成 `.agent/` 四件套、根目录 `docs/adr/` 和 README。禁止凭空编写或生成超出级别要求的文件。

---

# 六、需求分析规范

任何需求必须拆分为：

```
业务需求层 → 领域模型层 → 应用服务层 → 技术实现层 → 基础设施层
```

禁止：业务描述直接映射技术方案。

错误示范：

```
用户："我要开发商城系统"
AI："使用 Vue + SpringBoot + MySQL"   ← 错误，跳过了业务分析
```

正确流程：先分析业务（用户、商品、订单、支付、库存、营销），再设计技术（前端框架、后端框架、数据库、缓存、消息、权限、部署）。

产品模式下，此阶段输出物为**领域模型草图 + 用户核心流程**（使用 `templates/domain-model-template.md` 生成），需用户确认后才进入技术选型。

---

# 七、技术选型总览

技术选型采用**逐层交互式确认**模式：每轮只询问一个选型问题，用户确认后必须基于全部累计选择重新 `web_search` 调研，再生成下一轮候选。

> **完整执行协议：** 强制要点、进度追踪格式、结果汇总、变更流程、异常处理与断点恢复的详细定义见 `references/execution-protocol.md`。**执行技术选型时必须加载该文档。** 多选兼容评估（0.5 协议）、兼容性矩阵等见 `references/tech-stack-guide.md`。

## 7.1 选型流程导航（四阶段）

选型分四阶段，阶段内逐次单问：

- **阶段一：核心选择（跨端）** — 前端主框架、后端运行时、后端开发语言、数据库、部署方案。**后端运行时与开发语言必须作为两个独立问题**
- **阶段二：前端层级（仅前端）** — 主框架 → 语言 → 构建工具 → UI 组件库 → CSS → 网络请求 → 状态管理 → 路由 → 表单验证 → 测试 → 代码质量 → 国际化（按需）。全部完成后先执行"前端-配套插件推荐"，确认后才开始后端选型
- **阶段三：后端层级（仅后端）** — 框架 → API 规范 → ORM/数据访问 → 数据库 → 缓存 → 消息队列 → 认证 → 日志 → 测试 → 文件存储（按需）→ 定时任务（按需）。全部完成后先执行"后端-配套插件推荐"，确认后才进入阶段四
- **阶段四：跨端关注点** — CI/CD、错误监控/APM、安全扫描/SAST/DAST

## 7.2 核心红线（选型时始终遵守）

- 每轮只问一个问题，用户确认后重新 `web_search`，禁止复用旧结果
- AI 只推荐不代替选择，禁止自动填充、自动锁定、静默跳过层级
- 版本实时查询，禁止沿用历史版本号（运行时选 LTS）
- 已选框架覆盖的层级仍需询问（推荐"使用内置"并标注"已覆盖"）
- 一个选项只代表一个独立决策，禁止预先绑定组合（如 `Node.js + TypeScript`）
- 遇异常必须回退并继续，禁止只报错不恢复（详见 `execution-protocol.md` 第五章）

## 7.3 异常处理核心原则

任何异常必须用"⚠️问题→✅已处理→👉下一步"三段式提示用户，**禁止只报错不恢复、禁止让用户从头开始**。进度持久化到 `.agent/selection-progress.md`（M 级及以上自动写），断点可恢复。详细恢复协议（含进度文件损坏、多 Agent 并发、误跳过层级等 9 种故障）见 `references/execution-protocol.md` 第五章。

---

# 八、项目整体架构

L 级项目统一采用：**Core + Business Module + Plugin + Infrastructure + Documentation + Test**

```
project
├── core              # 核心基础能力（前后端结构不同）
├── modules           # 业务模块（DDD）
├── plugins           # 插件扩展
├── infrastructure    # 基础设施层
├── config            # 全局配置
├── docs              # 文档（含 ADR）
├── tests             # 测试
└── README.md
```

> **完整架构规范：** Core 设计原则、业务模块 DDD 四层结构、模块通信模式、复用规则、ADR、Monorepo 等详细定义见 `references/execution-protocol.md` 第六章与 `references/architecture-design.md`。

---

# 九、AI 开发生命周期

项目任务必须根据规模与风险执行对应生命周期：

- **S 级低风险**：理解 → 实现 → 验证 → 总结
- **M 级低风险**：理解 → 分析 → 设计 → 实现 → 验证 → 总结
- **L 级或任意高风险**：完整六阶段 + 安全、发布和回滚门禁

详细步骤见 `references/dev-lifecycle.md`。任何被裁剪的阶段都必须在总结中说明原因，不能静默跳过。

---

# 十、关键红线（各模式通用）

- **测试**：新功能必须有单元测试 + 集成测试，覆盖正常/异常/边界
- **安全**：禁止硬编码密码、提交 API Key、暴露数据库账号、输出敏感日志；必须环境变量管理、权限控制、输入验证、日志脱敏
- **Core 修改**：必须说明修改原因、影响范围、兼容方案、迁移方案
- **删除代码**：先检查引用关系、历史兼容、API 影响
- **README**：项目必须包含 README.md（8 要素见 `references/dev-lifecycle.md` 第 6 节）

---

# 十一、何时不使用与 FAQ

## 何时不使用

- **一次性脚本 / 临时工具**：按 S 级精简流程（理解→实现→验证→总结），无需完整选型与架构
- **纯技术验证 Demo**：跳过架构设计与 ADR，直接验证可行性
- **用户已定技术栈**：走"已有项目反向分析"（tech-stack-guide.md 第 6 节），只评估与优化，不重新选型
- **与架构无关的问答**：直接回答，不触发本 Skill 流程

## FAQ

| 问题 | 答案 |
|------|------|
| 用户已有既定技术栈，还要选型吗？ | 不需要。走"已有项目反向分析"，只评估优化 |
| 不支持 `ask_followup_question` 怎么办？ | 回退文字列表选型：正文逐层列出候选，用户回复编号/名称确认 |
| 项目很小，必须走完整流程吗？ | 不必。按规模分级执行 S/M 精简流程与门禁 |
| 选型到一半用户改主意？ | 走变更流程：评估影响→提示→确认→重选受影响层级（见 `execution-protocol.md` 第四章） |
| `web_search` 查询失败或超时？ | 回退到 tech-stack-guide.md 内置版本基线，标注"可能滞后"，不阻塞选型（见 `execution-protocol.md` 第五章 / faq-deep Q17） |
| 选型中途断开或 AI 报错中断？ | 进度已存 `.agent/selection-progress.md`，新会话自动检测并提示恢复，不用从头开始（见 `execution-protocol.md` 第五章） |
| 国内网络环境能用吗？ | 能。版本查询、依赖安装、Git 克隆、部署均有国内回退方案（见"环境与工具适配"表） |

---

# 十二、参考文档索引

| 文档 | 加载时机 |
|------|----------|
| `references/execution-protocol.md` | **执行技术选型、处理选型异常、设计项目架构时必须加载** |
| `references/tech-stack-guide.md` | 查询多选兼容评估协议、兼容性矩阵、移动端选型时 |
| `references/architecture-design.md` | 深入设计项目架构或规划模块结构时 |
| `references/dev-lifecycle.md` | 编写代码、创建测试、建立开发标准时 |
| `references/examples.md` | 理解本 Skill 的完整使用方式时 |
| `references/sample-reports.md` | 需要参考完整输出格式（领域模型/技术栈汇总/反向分析/验收）时 |
| `references/anti-patterns.md` | 技术选型、架构设计、持续开发时对照自检 |
| `references/faq-deep.md` | 遇到边界情况、多 Agent 协作等边缘问题时 |
| `templates/` | 新项目初始化生成 `.agent/`、ADR、README 时 |
| `templates/domain-model-template.md` | 产品模式下需求分析阶段，输出领域模型草图时 |

---

# END
